const age = 19
const isOldEnough = age >= 18

console.log(isOldEnough)

if (isOldEnough) {  //if isOldEnough == true) then it prints.
    console.log('Vikas can start driving license 🚘') //CMD+CTRL+SPACE to get Sticker like smiley, car...etc
}

else {
    console.log('Not permittable age')
}